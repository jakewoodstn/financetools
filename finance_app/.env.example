# Local dev only — copy to .env (gitignored).
# Production config: GitHub Environments → see docs/github-setup.md

DATABASE_URL=postgresql+psycopg://finance:finance@localhost:5432/finances
APP_DEBUG=true
